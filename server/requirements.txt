fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20
asyncpg>=0.30
greenlet>=3.0
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
python-multipart>=0.0.9
# Locating each call's raw agent log in Blob Storage (app/azure_logs.py). The sync
# client is used from a thread, which is why the [aio] extra isn't needed.
azure-storage-blob>=12.19
# Client-side websockets for the automated test caller (app/testcall/realtime.py).
# uvicorn[standard] already pulls this in for the *server* side; naming it here is
# what stops a slimmer uvicorn install silently breaking test calls only.
websockets>=12.0
